import { timeout, after, pace, sleep, until, select } from '../async.js';

Promise.timeout = timeout;
Promise.after = after;
Promise.pace = pace;
Promise.sleep = sleep;
Promise.until = until;
Promise.select = select;
//# sourceMappingURL=promise.js.map
