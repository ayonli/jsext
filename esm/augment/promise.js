import { timeout, after, pace, sleep, until, select, yieldNow } from '../async.js';

Promise.timeout = timeout;
Promise.after = after;
Promise.pace = pace;
Promise.sleep = sleep;
Promise.until = until;
Promise.select = select;
Promise.yieldNow = yieldNow;
//# sourceMappingURL=promise.js.map
