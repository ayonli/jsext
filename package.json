{
    "name": "@ayonli/jsext",
    "version": "0.1.0",
    "description": "Extended functionalities that are missing in the JavaScript standard types but frequently used in practice.",
    "main": "index.js",
    "types": "index.ts",
    "scripts": {
        "prepublishOnly": "tsc",
        "test": "mocha",
        "postpublish": "ls *.js|xargs rm; ls *.js.map|xargs rm; ls collections/*.js|xargs rm; ls collections/*.js.map|xargs rm"
    },
    "devDependencies": {
        "@types/mocha": "^10.0.1",
        "@types/node": "^20.6.0",
        "dotry": "^1.4.0",
        "mocha": "^10.2.0",
        "ts-node": "^10.9.1",
        "typescript": "^4.9.5"
    },
    "engines": {
        "node": ">=14"
    },
    "mocha": {
        "require": [
            "ts-node/register"
        ],
        "spec": [
            "*.test.ts",
            "**/*.test.ts"
        ]
    }
}
